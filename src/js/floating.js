function floating() {
    // let listMenu = document.querySelector(".header-item__list");
    // let listItems = document.querySelectorAll(".header-item__list-item");
    // let floatingMenu = document.querySelector(".header-item__floating-menu");
    // let startWidth = "";

    // startWidth = window.getComputedStyle(listItems[0], null).getPropertyValue("width");

    // floatingMenu.style.width = startWidth;
    
    // listMenu.addEventListener("mousemove", (e) => {
    //     if(e.target && e.target.className == "header-item__list-item" && e.target.children.length == 0) {

    //     }
    // });
}

export default floating;